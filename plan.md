# 当前计划

## CARD-only 支线改造

- [x] 创建 `codex/card-only` 长期支线并锁定完全无状态边界。
- [x] 移除 MVU 引擎、变量入口、变量协议与插件。
- [x] 将发送、状态栏、流式与完成生命周期改为纯消息接口。
- [x] 将构建器收缩为固定 8 条规则并改用 `card-*` 产物。
- [x] 更新作者文档、API、AI 工作流与项目规则。
- [x] 运行构建、JSON 语法检查和 MMD 新页校验。

## 新作者任务模板

- [ ] 通过对话确认角色设定与开场白。
- [ ] 确认全局美化后修改第一组界面入口。
- [ ] 确认功能栏、回复状态栏与可选舞台后修改第二组界面入口。
- [ ] 确认发送、流式与完成行为后修改第三组行为入口。
- [ ] 运行构建与 MMD 新页校验；作者明确要求时再生成预览。

## 维护规则

- 作者需求只改 `src/author/`；框架修改必须另行说明。
- 每次修改立即更新 `main.md`，完成步骤立即勾选本文件。
- 不在本支线重新引入跨轮剧情状态；需要变量系统时使用 `main` 分支。
