<角色设定>
请在此填写角色身份、行为规则、世界设定和写作要求。

{{VARIABLE_RULES}}

<变量协议>
用户消息末尾的[当前变量]包含本轮起始YAML变量，以它为准。
每轮先输出普通正文，再在末尾输出且只输出一个<变量更新>块；块内必须是严格JSON数组。
支持四种操作：replace替换已有路径，delta对已有有限数字加减，insert向已有对象或数组新增值，remove删除已有路径。
每项操作格式为{"op":"操作名","path":"/JSON/Pointer"}；除remove外还必须提供"value"。
path必须是非空JSON Pointer。字段中的~写为~0，/写为~1；数组索引使用0或不带前导零的正整数，末尾插入使用-。
父节点必须存在，不自动创建；禁止__proto__、prototype、constructor路径片段。
字段名以_开头的变量及其子树只读。不要整体替换或删除包含只读字段的容器。
批量操作按顺序执行，任一操作、派生或Schema校验失败则整批不提交。
没有变化时输出<变量更新>[]</变量更新>。
更新块之后不得继续输出文字，不得使用Markdown代码围栏，不输出其他变量协议标签。
</变量协议>
</角色设定>
