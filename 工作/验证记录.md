# 最近验证

日期：2026-09-15

- `npm test`：20/20 通过。
- 默认构建：15 条规则；无插件基础包 12 条；加入 `manager` 为 16 条。
- `npm run build` 与 `npm run preview:fixture`：通过。
- 正式 JSON：语法合法；MMD 新页校验 0 错误、5 个已审核静态警告、37 项通过。
- chat 和 thin-preview：无页面脚本错误；默认三插件的报告、变量折叠和旧变量清理已同场复验。
- 功能栏分离检查：正式包不含内置功能栏；`status.html`、`status.js` 与“作者配置·回复状态栏”仅保留逐回复模板和入口。
- 变量规则检查：成品 persona 835 字；紧凑中文 YAML 规则标签唯一，默认无业务实例，不含 SillyTavern 专用标签、宏或 `add` 协议。

5 个警告均来自合并规则内相邻的脚本体顶层订阅，未在 `message:mount` 回调中嵌套订阅；`ready` 仅用于 MVU 就绪门控。
