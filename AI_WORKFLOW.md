# AI 自然语言协作流程

本工程面向不懂代码的作者。作者只需要说清楚想做什么，AI 负责把需求转换为变量、规则和前端实现；不要让作者填写 Schema、字段类型、JSON Pointer、SDK、生命周期或插件名称。

## 开始对话

把项目目录交给支持本地文件编辑的 AI，并发送：

```text
请使用 tavern-mmd 技能，目标平台固定为 MMD 新聊天页（/mmdsandbox，chatVersion: 1）。

我想制作：[用自然语言描述角色、世界、玩法、希望系统记住的内容、界面和风格；暂时没想好的可以不写]

请先通过对话了解我的需求，不要立即改文件。不要问我代码概念，也不要重复询问我已经说明的信息。只追问尚未明确并且会改变实际玩法或界面结果的选择。

需求明确后，按本项目 AI_WORKFLOW.md 的五组流程逐组进行。每组先用简短、非技术语言说明准备实现的玩法与界面结果，等我明确确认后，才修改该组文件。完成全部五组后生成预览，让我做最终视觉确认。
```

作者不需要一次把所有内容想完整。AI 应把模糊描述整理成少量会影响结果的问题，一次只推进当前组。

## AI 工作规则

### 读取范围

常规制作先完整阅读：

1. `main.md`、`plan.md`
2. `README.md`、`AUTHORING.md`
3. `API.md`、`AI_WORKFLOW.md`
4. `src/author/` 中与当前组有关的入口

常规作者需求不读取 `src/framework/`，也不主动读取平台长文。只有以下情况才扩大范围：

- `API.md` 没有覆盖所需能力
- 验证出现无法由项目文档解释的错误
- 实际行为与项目文档不一致
- 作者明确要求修改或诊断框架

若适用技能明确要求读取平台资料，仍须遵守技能；本项目不修改全局 `tavern-mmd` 技能，因此严格技能路由的 AI 仍可能读取完整 `mmd-sandbox.md`。

### 提问边界

- 只问会改变产品结果的问题，例如要记住什么、何时变化、玩家能否操作、界面展示什么、偏好的视觉感受。
- 不问 Schema、字段名、字段类型、JSON Pointer、SDK、事件、生命周期、正则或插件名。
- 已有信息足以安全决定时直接整理为方案，不为形式确认重复提问。
- 不展示大段代码；每次确认只展示业务含义、玩家操作和可见结果。
- 未得到当前组确认前，不修改该组文件，也不提前实现后续组。

## 五组确认与实施

### 第一组：MVU 数据模型

先确认系统需要长期记住的事实、数值、关系、清单和变化规则。AI 用自然语言展示“会记住什么、谁能改变、何时自动计算”，作者确认后再修改：

- `initial-variables.json`
- `model.js`
- `variable-rules.txt`
- 与变量协议相关的 `persona.txt`

作者不清楚该追踪什么时，AI 先给出 2–4 个候选方案，每个只说明“会记住什么、会影响哪些剧情/玩法/界面”，不展示字段类型或 Schema。作者确认后，AI 再自行完成以下设计：

- 只保留有实际用途且无法可靠从其他字段推导的状态。
- 变量路径和业务字段使用简短中文；固定玩家键写“玩家”。
- 稳定命名的角色、任务和物品优先用对象键；只有顺序或重复有意义时用数组。
- 枚举只用于驱动阶段、分支或界面的有限状态；派生值统一交给 `derive`。
- 用紧凑中文 YAML 编写更新规则，只写模型无法可靠推断的类型、范围、格式、取值和更新条件。

AI 保证初始变量、严格 Schema、派生逻辑、更新规则和变量注入含义一致；字段增删改时同步传播，不采用 SillyTavern 的 Zod 4 转换、补值或容错写法。

### 第二组：变量注入

先确认每次玩家发送时，AI 需要看到哪些当前状态、哪些内容不应发送。默认全部发送，可基于上下文、变量状态等因素判断是否发送。作者确认后再修改 `variable-inject.js`，必要时同步变量说明。

### 第三组：全局美化

先确认整体氛围、深浅色倾向、色彩、圆角、密度和背景感受。作者确认可见风格方案后再修改 `theme.css`。聊天页、状态栏和舞台使用同一套视觉语义。

### 第四组：功能栏、回复状态栏与可选舞台

先确认是否需要顶层功能栏，以及其中有哪些侧边按钮、弹窗、菜单、侧边栏和快捷操作；再确认每条回复旁要保留什么冻结信息。只有作者需要覆盖消息区或整屏的大型界面时，才继续确认舞台及其开关；不要把舞台作为基础界面的默认方案。作者确认页面信息架构后再修改：

- `status.html`、`status.js`
- `stage.html`、`stage.js`
- 作者自行命名的可选功能栏文件、独立正则和 `statusbar` 唯一标记

顶层功能栏承载基础常驻入口与浮层，回复状态栏表现该回复的冻结状态；舞台仅承载地图、背包、小游戏或大型角色面板等高级界面。功能栏不使用 `status.html`、`status.js`，也不增加默认规则；只有作者确认需要时，才另建任意命名的源码文件、独立正则和标记。旧版 MMD 可以复用按钮、弹窗和侧边栏的交互思路，但实现必须改用新页能力。

### 第五组：发送、流式与完成行为

先确认发送前是否整理玩家文字、生成中显示什么、完整回复后哪些长期界面更新。作者确认交互结果后再修改：

- `before-send.js`
- `stream.js`
- `render.js`
- 必要的 `beginning.txt` 与 `persona.txt`

选项默认写入输入框让玩家确认发送；只有作者明确要求自动发送时才使用消息发送能力。

每组修改后立即更新 `main.md`，并在 `plan.md` 勾选刚完成的步骤。

## 插件建议

默认启用三项效果：

- 展示 AI 变量更新的诊断结果（`report`）
- 在玩家消息中折叠显示当轮变量（`user-view`）
- 新回合后清理较旧玩家消息中的变量块（`prune`）

对话中允许作者明确关闭任一效果。`manager` 不主动推荐；只有作者提出手动查看、修改或重置变量时，才说明变量管理面板的可见效果并询问是否需要。

AI 修改的是 `mvu.config.json`，不要求作者接触插件名称。

## 验证与交付

五组全部完成后：

1. 运行 `npm run build` 并生成本地仿真预览。
2. 对正式 JSON 做语法检查并运行 MMD 新页校验。
3. 在 chat 与 thin-preview 中检查桌面和窄屏；逐条审核警告。
4. 把预览交给作者做最终视觉确认；作者提出调整时，只回到受影响的组重新确认。

最终答复保持简短，只列：

- 交付文件
- 构建和校验结果
- 已审核警告
- 导入与 persona 粘贴步骤
- “必须新建角色并使用新聊天页、在全新对话中验收”的提醒

不修改 `src/framework/`，不添加兼容、兜底、迁移、默认补值或跨平台分支。
