<变量含义与更新规则>
#变量路径和字段用简短中文；玩家固定写玩家，不用名称宏作键
#可用项:类型、范围、格式、取值、分段、说明、更新；文本类型、自明含义、普通更新省略
#同类固定字段用${字段甲|字段乙}合并；动态集合写容器路径，类型中说明键和值
#仅据当前变量与本轮已发生事实更新，不预测；数值写单次幅度、阈值、显著事件
#_字段由MVU_MODEL.derive计算，$字段不注入，均不列
规则: {}
</变量含义与更新规则>
